<style>
    .custom-line {
        display: flex;
        justify-content: center;
        gap: 10px;
        border: none;
        border-top: 2px solid #000;
        width: 100%;
        margin: 1px;
        padding: 10px 0;
    }

        .custom-line button {
            padding: 20px 20px;
            background-color: transparent;
            color: black;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            position: relative;
        }

            .custom-line button::after {
                content: '';
                position: absolute;
                left: 0;
                bottom: 0;
                width: 0;
                height: 2px;
                background-color: red;
                transition: width 0.3s ease;
            }

            .custom-line button:hover::after {
                width: 100%;
            }

    .image-section {
        text-align: center;
        margin-top: 20px;
    }

        .image-section img {
            max-width: 100%;
            height: auto;
            margin-bottom: 50px;
        }

    .small-image-section {
        text-align: left;
        margin-top: 20px;
    }
    
    .small-image {
        width: 25%; 
        height: auto;
    }

    .custom-line-gray {
        border: none;
        border-top: 2px solid #ccc;
        width: 100%;
        margin: 1px;
        padding: 10px 0;
    }

    .gray-background-section {
        background-color: #f0f0f0;
        padding: 20px;
        margin-top: 20px;
        margin-bottom: 40px;
        text-align: center;
    }

        .gray-background-section h2 {
            margin-top: 20px;
        }

    .image-row {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-top: 20px;
    }

        .image-row img {
            width: 30%;
            height: auto;
            border-radius: 10px;
        }

    .dark-gray-box-row {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-top: 20px;
    }

    .dark-gray-box {
        background-color: #d0d0d0;
        padding: 20px;
        width: 30%;
        text-align: left;
        border-radius: 10px;
    }

        .dark-gray-box h3 {
            font-size: 16px;
            margin: 0;
        }

        .dark-gray-box h4 {
            font-size: 10px;
            margin: 0;
        }

    h5 {
        font: 16px;
        margin: 0;
    }

    .header-buttons {
    position: absolute;
    top: 80px;
    right: 20px; 
    display: flex;
    gap: 10px;
}

.header-button {
        padding: 10px 20px; /* 버튼 내부의 텍스트와 테두리 사이의 여백 설정 */
        background-color: #fff; /* 버튼 배경색을 흰색으로 설정 */
        color: #000; /* 버튼 텍스트 색을 검정색으로 설정 */
        border: 2px solid #000; /* 버튼의 테두리를 검정색 2px로 설정 */
        border-radius: 5px; /* 버튼의 모서리를 5px로 둥글게 설정 */
        cursor: pointer; /* 마우스를 올리면 커서가 포인터로 바뀜 */
        text-decoration: none; /* 링크의 기본 밑줄을 제거 */
        transition: background-color 0.3s ease, color 0.3s ease; /* 마우스 호버 시 색상 전환 효과 추가 */
        text-align: center; /* 버튼 텍스트를 가운데 정렬 */
        font-size: 16px; /* 버튼 텍스트 크기를 16px로 설정 */
}

.header-button:hover {
            background-color: #000; /* 배경색을 검정으로 변경 */
            color: #fff; /* 텍스트 색상을 흰색으로 변경 */
}

    .session-info {
        position: absolute;
        top: 20px; /* 위에서 10px 떨어짐 */
        right: 150px; /* 우측에서 150px 떨어짐 */
        font-size: 18px;
        color: #333; /* 텍스트 색상 */
    }

    .logout-button {
        position: absolute; /* 부모 요소를 기준으로 절대적인 위치를 설정 */
        top: 5px; /* 부모 요소의 상단에서 10px 떨어진 위치 */
        right: 30px; /* 부모 요소의 오른쪽에서 10px 떨어진 위치 */
        padding: 10px 20px; /* 버튼 내부의 상하 10px, 좌우 20px 패딩 */
        border: 2px solid #007bff; /* 파란색 (#007bff)의 2px 두께 테두리 */
        border-radius: 5px; /* 테두리 모서리를 5px만큼 둥글게 */
        background-color: white; /* 버튼 배경색을 흰색으로 설정 */
        color: #007bff; /* 버튼 텍스트 색상을 파란색으로 설정 */
        text-decoration: none; /* 링크의 기본 밑줄 제거 */
        font-size: 16px; /* 글자 크기를 16px로 설정 */
        font-weight: bold; /* 글자 두께를 굵게 설정 */
        transition: background-color 0.3s ease, color 0.3s ease; /* 배경색과 글자색이 0.3초 동안 부드럽게 변화하도록 설정 */
    }
        .logout-button:hover {
            background-color: #007bff;
            color: white;
        }

    <button onclick="location.href='@Url.Action("Product_Info", "Home")'" > Product Info</button > /*제품 정보창넘어가기*/
  
   
    
</style>



<!-- 상단 고정 버튼 -->
<div class="header-buttons">
    <a class="header-button" asp-controller="Home" asp-action="Login">Login</a>
    <a class="header-button" asp-controller="Home" asp-action="SignUp">Sign Up</a>
</div>

<h3 class="session-info">@ViewBag.MySession 님 안녕하세요</h3> <!--로그인 했는지 안했는지-->
<a asp-controller="Home" asp-action="Logout" class="logout-button">Logout</a>

<div class="small-image-section">
    <a href="@Url.Action("Index", "Home")">
        <img src="./SimBol.png" alt="Simbol Image" class="small-image">
    </a>
</div>

<h1>Welcom To ANU Energy Solution!</h1>

<div class="custom-line">
    <button onclick="location.href='@Url.Action("Product_Info", "Home")'">Product Info</button>
    <button onclick="location.href='@Url.Action("NewsAndEvents", "Home")'">News & Events</button>
    <button onclick="location.href='@Url.Action("Enquiry", "Home")'">Enquiry</button>
</div>

<div class="custom-line"></div>

<p></p>

<h1>Introduction</h1>

<div class="image-section">
    <img src="./grid_intro_main.png" alt="Grid Introduction Image">
</div>

<h2>Application of ESS</h2>

<div class="custom-line-gray"></div>
<p></p>

<div class="image-section">
    <img src="./grid_intro_app.png" alt="Grid Intro Image">
</div>

<div class="gray-background-section">
    <h2>Grid-scale and C&I</h2>
    <p>
        Energy solutions for commercial and industrial-scale applications.
    </p>

    <div class="image-row">
        <img src="./grid_scale01.png" alt="grid_scale01">
        <img src="./grid_scale02.png" alt="grid_scale02">
        <img src="./grid_scale03.png" alt="grid_scale03">
    </div>

    <div class="dark-gray-box-row">
        <div class="dark-gray-box">
            <h3>Peak Shifting</h3>
            <h4>*Charge during off-peak times</h4>
            <h4>*Discharge during peak times</h4>
        </div>
        <div class="dark-gray-box">
            <h3>Renewable integration</h3>
            <h4>*Stabilize the intermittent renewable power by alternately charging and discharging</h4>
        </div>
        <div class="dark-gray-box">
            <h3>Frequency Regulation</h3>
            <h4>*Charge when grid frequency increases</h4>
            <h4>*Discharge when grid frequency decreases</h4>
        </div>
    </div>
</div>

<h1>Grid-scale and C&I</h1>

<h1>ANU Energy Solution Key Features</h1>
<div class="custom-line"></div>
<img src="./grid_intro_key.png" alt="grid_intro_key">
